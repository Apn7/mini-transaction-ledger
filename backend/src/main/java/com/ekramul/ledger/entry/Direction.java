package com.ekramul.ledger.entry;

/** Which way money moved for the account on a ledger entry. */
public enum Direction {

	/** Money left the account. */
	DEBIT,

	/** Money arrived in the account. */
	CREDIT
}
