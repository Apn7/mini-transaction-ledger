# Internal working notes - not part of the submission
_notes/

# Secrets and local environment
.env
.env.local
*.env
!.env.example

# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node / Angular
node_modules/
dist/
.angular/
npm-debug.log*

# IDE and OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# Local Claude Code config - not part of the submission
.claude/

# Angular
out-tsc/
coverage/
.history/

# LaTeX build artifacts. The source and the built PDF are both committed -
# the graders should not have to install a TeX distribution to read it.
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz
